// Grid constants
export const GRID_SIZE = 8;
export const WIDGET_GAP = GRID_SIZE * 2;

// Handle offset for group selection
export const HANDLE_OFFSET = 12;

// Viewport limits
export const MIN_SCALE = 0.5;
export const MAX_SCALE = 2;
export const MIN_X = -1000;
export const MAX_X = 1000;
export const MIN_Y = -1000;
export const MAX_Y = 1000;

// Zoom settings
export const ZOOM_SPEED = 0.001;